{
  "name": "poznamkovac_veci",
  "module": "poznamkovac.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build poznamkovac.ts --minify --outdir=dist --target=browser"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "version": "1.0.0",
  "description": "Pojmová mapa pre https://poznamkovac.eu",
  "dependencies": {
    "vis-network": "^9"
  }
}
